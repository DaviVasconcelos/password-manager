# AGENTS.md

Local password manager (Clean Architecture, C#). WinUI 3 desktop app; SQLite/EF Core storage (ADR 0003) and AES-256-GCM/Argon2id crypto (ADR 0004) implemented. Read `docs/adr/*` before touching domain, persistence, or crypto code.

## Design da UI (Figma)

- Plano de implementação do redesign: `docs/design/design-plan.md`. Fonte de verdade do design: `docs/design/figma-snapshot.md` (regenerável).
- Para atualizar o snapshot após mudanças no Figma: `powershell -ExecutionPolicy Bypass -File scripts/fetch-figma-design.ps1`. Requer env var `FIGMA_PERSONAL_ACCESS_TOKEN` (nunca commitar o token). Header da Figma REST API: `X-Figma-Token`.
- O snapshot `figma-snapshot.json` (cru, 10+ MB) é gitignored; só o markdown é versionado.

## Commands

- Build: `dotnet build PasswordManager.slnx -p:Platform=x64` (Debug, exige `x64` por causa do `PasswordManager.UI` unpackaged — `WindowsPackageType=None`, `EnableMsixTooling=false` — sem `-p:Platform=x64` o build cai em `win-arm64` e `UI.Tests` falha com `BadImageFormat`). Verified passing; tests at `dotnet test PasswordManager.slnx -p:Platform=x64`. Sem flag compila, mas `UI.Tests` só roda com `x64` no host `windows-latest`.
- Single test project: `dotnet test tests/PasswordManager.Domain.Tests/PasswordManager.Domain.Tests.csproj` (xUnit + FluentAssertions). Same for `tests/PasswordManager.Infrastructure.Tests/PasswordManager.Infrastructure.Tests.csproj`, `tests/PasswordManager.Application.Tests/PasswordManager.Application.Tests.csproj` e `tests/PasswordManager.UI.Tests/PasswordManager.UI.Tests.csproj -p:Platform=x64` (`net8.0`, `UseWinUI=false` mas referencia `PasswordManager.Presentation` — sem dependência de empacotamento MSI/MSIX, exige `x64` apenas por `PasswordManager.UI` transitivamente no CI).
- The solution is `.slnx`, not `.sln`. It requires the SDK pinned in `global.json` (10.0.302, `rollForward: latestFeature`). Projects target `net8.0`; SDK 10 builds them fine — do not "fix" the TFM.
- `src/PasswordManager.UI.slnx` is a UI-only solution (unpackaged, distribuição via **MSI/WiX** — `WindowsPackageType=None`); the UI needs Windows/App SDK tooling and platform mapping (x86/x64/ARM64), so it is not runnable headless from CLI. Gerar MSI: `dotnet publish src/PasswordManager.UI -c Release -p:Platform=x64 -r win-x64 --self-contained -o publish` + `installer/generate-AppFiles.ps1` + `wix build -arch x64 -d PublishDir=publish -o PasswordManager.msi installer/Package.wxs installer/AppFiles.wxs` (ver `installer/Package.wxs:1`).
- CI/CD: GitHub Actions, `.github/workflows/ci.yml` (ADR 0006). `windows-latest`; on every push/PR it runs `dotnet ef migrations has-pending-model-changes` + `dotnet build PasswordManager.slnx -p:Platform=x64` (Debug) + `dotnet test PasswordManager.slnx -p:Platform=x64` (Debug, `--no-build`) nos 4 projetos (`Domain 57` + `Infrastructure 66` + `Application 66` + `UI 58` = 247), sobe TRX (14 dias) e em job separado `build-msi` publica `publish` self-contained e gera `PasswordManager-0.1.0-x64.msi` via WiX 5 (`installer/Package.wxs`, `UpgradeCode` estável, `MajorUpgrade`) como artefato (30 dias). `setup-dotnet` lê `global.json` (SDK 10.0.302) e instala `8.0.x` para os testes. MSI é **sem assinatura** (unsigned, next-next).

## Architecture

- Clean Architecture: `src/PasswordManager.Domain` (no deps), `Application` (interfaces + `VaultSessionService` + password generation), `Infrastructure` (CryptoService + EF Core persistence), `Presentation` (`net8.0` — ViewModels + Services abstraídos, sem dependência WinUI), `UI` (WinUI 3 + MVVM, referencia Presentation).
- `Vault` is the DDD aggregate root (ADR 0001). `VaultItem`/`VaultFolder` are child entities mutated ONLY through `Vault` methods (`AddItem`, `RemoveItem`, `UpdateItem`, `AddFolder`, `RemoveFolder`, `RenameFolder`, `AssignItemToFolder`, `MergeFrom`). Never expose or mutate `_items`/`_folders` directly; there is no `IVaultItemRepository`.
- Persistence/deserialization goes through `internal` `Rehydrate` factories on entities (enabled by `InternalsVisibleTo` in Domain). `RemoveFolder` must NOT delete its items — it only clears `FolderId`.
- Storage is a single encrypted JSON blob (ADR 0003): `serialize whole Vault ->` `encrypt with AES-256-GCM ->` one SQLite row (insert on `CreateAsync`, update on `SaveAsync`/`ChangeMasterPasswordAsync`). Implemented by `VaultRepository` (Infrastructure). Search/filter happens in memory after decrypt.
- `IVaultRepository` receives the already-derived AES key, never the master password: key derivation is an Application concern (`VaultSessionService` derives via `ICryptoService` using the stored salt). The session retains only the derived key in memory and zeroes it on lock (`CryptographicOperations.ZeroMemory`). `CreateAsync` throws if a vault already exists; `SaveAsync` requires an existing record (keeps the salt); `ChangeMasterPasswordAsync` rotates salt + blob.
- `IVaultSessionService` centralizes session + CRUD (itens/pastas com auto-save) + `BuscarItens` (filtro em memória) + `ExisteCofreAsync`. `IPasswordGenerator` (RandomNumberGenerator, sem viés de módulo) e `IPasswordStrengthEvaluator` (enum `ForcaSenha`) ficam em `Application/PasswordGeneration`. A UI fala só com o serviço de sessão; o `Vault` carregado fica na sessão e as mutações são persistidas a cada operação.
- Presentation/UI desacoplamento: ViewModels em `PasswordManager.Presentation` com abstrações `IClipboardService`/`ITimer`/`ITimerFactory`/`IIdiomaProvider`/`ILocalizationService` para testabilidade; `PasswordManager.UI` contém apenas Views, `Services/*` reais (`ClipboardService`, `DispatcherQueueTimerAdapter`, `ApplicationLanguagesProvider`) e `App.xaml.cs` (DI + `AplicarTema`/`AplicarIdiomaPreferencial`). Testes em `PasswordManager.UI.Tests` usam fakes (`FakeTimer`, `FakeClipboardService`, etc.).
- Crypto (ADR 0004): `ICryptoService` implemented by `CryptoService` (Infrastructure) — Argon2id (`Konscious.Security.Cryptography.Argon2`) defaults to 64 MiB/3 iter./4 parallel, salt 16 bytes, key 32 bytes; AES-256-GCM package is `nonce(12) + tag(16) + ciphertext`. Argon2 params are constructor-injectable for fast tests — never weaken them in production code. Any GCM tag failure throws `CryptographicIntegrityException` (wrong master password vs. tampered data are indistinguishable on purpose).
- Export/Import (ADR 0005): `IExportImportService` (Application) + `ExportImportService` (Infrastructure). Arquivo `.vault` autocontido: `magic "PMVT"` + versão + salt novo por exportação + pacote AES-GCM (reusa `VaultDataMapper` + `ICryptoService`), criptografado com a senha mestra re-digitada. `IVaultSessionService.ExportAsync`/`ImportAsync` orquestram; no import o usuário escolhe substituir ou mesclar (`Vault.MergeFrom`: pastas por nome case-insensitive, itens deduplicados por título+usuário, novos GUIDs); import com sessão trancada só cria cofre na primeira execução. I/O de arquivo (file pickers WinUI 3) fica na UI — camadas trocam bytes. Sem CSV nesta etapa.

## Conventions

- All code comments, exception messages, test names, and docs are in Brazilian Portuguese (pt-BR). Write new code/tests that way.
- `CryptographicIntegrityException` lives at `Application/Abstractions/CryptographicIntegrityException.cs` but is in namespace `PasswordManager.Application.Exceptions` — keep the namespace when importing.
- Entities use private parameterless ctors + static factories; `Guid` keys, `DateTime.UtcNow` timestamps, private setters.
- `VaultRepository` stores a fixed non-NULL singleton `Guid` (see `SingletonRecordId`). NEVER use `Guid.Empty` as a persisted key: Microsoft.Data.Sqlite binds `Guid.Empty` as BLOB while the column stores GUIDs as TEXT, so `WHERE Id = Guid.Empty` silently matches nothing (SQLite type affinity).
- Tests: `xUnit` + `FluentAssertions`. Test method names are `Method_Scenario_ExpectedResult` in pt-BR, e.g. `RemoveItem_ComIdInexistente_DeveLancarExcecao`.
- Distribuição: **MSI (WiX Toolset 5)** via `installer/Package.wxs` (nunca MSIX). `PasswordManager.UI.csproj` tem `WindowsPackageType=None` e `EnableMsixTooling=false`; `PublishTrimmed=false` (EF Core não é trimming-safe). `UpgradeCode` em `Package.wxs` é estável — não altere. Version em `Package/@Version` deve ser sincronizada com git tag.

## Current state

- Implemented: Domain entities (`Vault` + `MergeFrom`) + 57 domain tests; Infrastructure `CryptoService` + `VaultRepository` (SQLite/EF Core, ADR 0003) + `ExportImportService` (.vault, ADR 0005) + `AppSettingsService` (settings JSON) + 66 infrastructure tests; Application `VaultSessionService` (criar/desbloquear/trancar/trocar senha mestra/salvar/CRUD/busca/exportar/importar) + `PasswordGenerator`/`PasswordStrengthEvaluator` + `AppSettings` (validação) + fakes + 66 application tests; **UI ViewModels testáveis + 58 UI tests** (`VaultViewModel` 23 + `SettingsViewModel` 10 + `ItemEditorViewModel` 10 + `UnlockViewModel` 12 + 3 smoke, `PasswordManager.UI.Tests` `net8.0` com `FakeTimer`/`FakeClipboard`/`FakeIdiomaProvider`). UI funcional (WinUI 3 + CommunityToolkit.Mvvm + DI): `UnlockPage` (criar/desbloquear/importar backup na primeira execução) e `VaultPage` (lista, busca, filtro por pasta, CRUD via diálogos, copiar senha com limpeza em 30 s configurável, gerador + força com defaults configuráveis, auto-lock por inatividade, trocar senha mestra, exportar/importar `.vault` com substituir ou mesclar) + `SettingsContent` (Configurações). **Tema claro/escuro/sistema implementado** (`ea2a5f1`): `AppSettings.Tema` (`sistema`/`claro`/`escuro`, `AppSettings.cs:50`), `App.AplicarTema`/`AplicarTemaSalvo` (`App.xaml.cs:192`) com `RequestedTheme` + tema pendente, `SettingsViewModel.OpcoesTema` e persistência em `settings.json`; tokens em `Themes/DesignTokens.xaml`. Persistência real em `LocalAppData\PasswordManager\vault.db` (legado, a migrar para `Vaults/*.db` em `07-multi-arquivo-cofre`) via `VaultDatabaseMigrator.ApplyMigrations` (migration inicial `InitialCreate` + baseline para bancos legados; `dotnet-ef 8.0.30` em `.config/dotnet-tools.json`) e configurações em `LocalAppData\PasswordManager\settings.json` (global). CI/CD ativo (ADR 0006): GitHub Actions em `windows-latest` roda a checagem de migrations pendentes (`dotnet ef migrations has-pending-model-changes`), compila a solution e roda os 247 testes em cada push/PR, publicando os TRX como artefato (14 dias) e MSI `PasswordManager-0.1.0-x64.msi` (30 dias). **Próximo: Fase C — Múltiplos arquivos de cofre (ADR 0008, Opção B)** — `vaults.json` + `Vaults/*.db` + `IVaultRegistry` + `IVaultDbContextFactory`, `UnlockViewModel`/`UnlockPage` com lista de saves antes do unlock, nomes `vault-1`, `vault-2` com renomeação livre. Ver `docs/plans/07-multi-arquivo-cofre.md:1`.

## Roadmap

> Verificado no código em 2026-09-01 — Fase A e B 100% entregues. Distribuição MSI já implementada no CI (sem assinatura). Próximo: Fase C — Múltiplos arquivos de cofre (ADR 0008, Opção B) — ver `docs/plans/07-multi-arquivo-cofre.md`.

### Concluído — Fase A (Robustez/UX) e Fase B (Engenharia)

Todos os itens abaixo estão implementados e cobertos por testes (247 testes):

- **Fase A:** Configurações (`IAppSettingsService`/`AppSettingsService` + `settings.json` + `SettingsViewModel`/`SettingsContent`), Auto-lock por inatividade (2 min padrão, `ITimer`/`DispatcherQueueTimerAdapter` reiniciado a cada pointer/key), Trocar senha mestra (exige senha atual com verificação em tempo constante, `CryptographicIntegrityException`), Tema claro/escuro/sistema (`AppSettings.Tema`, `App.AplicarTema`/`AplicarTemaSalvo` + `RequestedTheme`, `DesignTokens.xaml`).
- **Fase B:** Migrations EF Core (`dotnet-ef 8.0.30`, `VaultDatabaseMigrator.ApplyMigrations` + baseline legado, `has-pending-model-changes` no CI), Testes de ViewModels (58 UI tests desacoplados via `IClipboardService`/`ITimer`/`IIdiomaProvider`, `PasswordManager.Presentation` + `PasswordManager.UI.Tests`), i18n (ADR 0007 — PRI + `Strings/<lang>/Resources.resw`, `ILocalizationService`, fallback `pt-BR`/`en-US`).

Detalhes e decisões: `AGENTS.md` histórico git + `docs/plans/06-testes-viewmodels.md`. Itens removidos do roadmap ativo para manter foco no que falta.

### Em aberto

#### Fase C — Features de produto

7. **Múltiplos arquivos de cofre (Opção B, ADR 0008) — EM IMPLEMENTAÇÃO** — 1 arquivo SQLite por cofre em `LocalAppData\PasswordManager\Vaults\*.db` + `vaults.json` (registry: id, nome, arquivo, criadoEm, atualizadoEm, ativoId) + `IVaultRegistry` + `IVaultDbContextFactory`. Nomes padrão `vault-1`, `vault-2`, ... com renomeação livre (validação/slug + colisão). Renomear/excluir sem desbloqueio (metadados não criptografados). Migração automática de `vault.db` legado -> `Vaults/vault-1.db`. Sessão tranca e zera chave ao trocar de arquivo. `UnlockViewModel`/`UnlockPage` com lista de saves antes do unlock. Configurações continuam globais (`settings.json`). Ver `docs/plans/07-multi-arquivo-cofre.md` e `docs/adr/0008-multi-arquivo-de-cofre.md`.

8. Import CSV (Bitwarden/LastPass/1Password). **Decisão**: **ADIADO** — ADR 0005 segue sem CSV (texto plano expõe senhas).
9. TOTP/2FA. **Decisão**: **incluir**; o secret fica criptografado dentro do item do cofre; a UI gera o código de 6 dígitos; geração de QR é enhancement futuro (entrada manual do secret primeiro).
10. Favoritos / tags / health check de senhas (força, reuso, expiração).

#### Fase D — Distribuição

11. **IMPLEMENTADO (sem assinatura)** — Empacotamento **MSI** com WiX Toolset 5 (`installer/Package.wxs` + `installer/generate-AppFiles.ps1`), publish self-contained `win-x64`, `UpgradeCode` estável + `MajorUpgrade`, MSI `PasswordManager-0.1.0-x64.msi` gerado no CI (`build-msi`, `wix 5.0.2`) e publicado como artefato. App é **unpackaged** (`WindowsPackageType=None`, `EnableMsixTooling=false`, `PublishTrimmed=false`). Próximos passos: assinatura de código/certificado, versionamento via git tag, e distribuição (winget/store interno).
12. Auto-update / backup automático / lembretes de backup — **pendente**.
